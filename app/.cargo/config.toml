# getrandom 0.3 needs an explicit backend on wasm32-unknown-unknown, or the WASM build
# fails to link. Scoped to the wasm target so native (keeper/indexer) is unaffected.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
