# Rust / Anchor build output (regenerated by build; includes IDL, .so, program keypair)
target/
**/target/
.anchor/
test-ledger/
.fingerprint/

# Node
node_modules/
**/node_modules/

# Dioxus / web build
dist/
**/dist/
.dioxus/

# Logs & scratch
*.log
.docker-build.log
.anchor-build.log
.orao-build.log
verdict.json

# Secrets — NEVER commit keypairs or env
.env
*.env
**/*-keypair.json
**/id.json
**/devnet.json
**/*.pem

# Arcium: compiled circuits (large, regenerated) + localnet artifacts (contains node keypairs)
path-b/build/
path-b/artifacts/
